import React from 'react';
import MovieList from './movie/MovieList';

const MovieMain = () => {
    return (
        <div>
            <MovieList></MovieList>
        </div>
    );
};

export default MovieMain;